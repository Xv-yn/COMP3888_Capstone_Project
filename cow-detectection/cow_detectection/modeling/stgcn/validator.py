"""
Validator for ST-GCN training.

Implements the STGCNValidator class, which inherits from BaseValidator.
Provides functionality for stratified K-fold cross-validation of the
ST-GCN model, including training on multiple splits, evaluating metrics
(e.g., accuracy, F1), and selecting the best performing model checkpoint.
"""
